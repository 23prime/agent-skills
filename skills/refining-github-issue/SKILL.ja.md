---
name: refining-github-issue
description: GitHub Issue の内容が曖昧、受け入れ条件が不明確、またはスコープを実装前に整理したいときに使用する。
translated_from: SKILL.md
---

# GitHub Issue のブラッシュアップ

指定された GitHub Issue を取得し、ユーザーとの対話を通じて内容をブラッシュアップするスキル。
実装に着手する前に Issue の意図・スコープ・受け入れ条件を明確にすることが目的。

## ワークフロー

### 1. 実行モードを選ぶ

**軽量（デフォルト）**：以降のワークフローを現在のスレッド・モデル上でそのまま実行する。

**重量**（Issue が特に曖昧または重要度が高い場合に使う。判断に迷えばユーザーに確認する）：`issue-refiner` エージェント（フォアグラウンド）をより高い推論努力でインタビューに起動し、続けて `issue-reviewer` エージェント（フォアグラウンド）でその結果をフレッシュな目で二重チェックする。起動方法は各エージェント自身の説明を参照する。このスキルのステップ2〜5は `issue-refiner` の内部で実行されるものなので、重量モードでは同じ内容をインラインで重ねて実行しない。

### 2. Issue を取得する

Issue 番号または URL を受け取り、`gh` CLI で取得する。

```bash
# 番号指定
gh issue view <number> --json number,title,body,labels,assignees,comments

# URL 指定の場合はリポジトリを自動解決
gh issue view <url> --json number,title,body,labels,assignees,comments
```

リポジトリが明示されていない場合は、カレントディレクトリの remote origin を使う。

### 3. Issue を分析する

取得した Issue の本文と**コメント**を読み込み、以下の観点で不明確な点・曖昧な点を洗い出す。

- **目的**：なぜこの Issue が必要か。
- **スコープ**：どこまでが対象か（何を含まないか）。
- **受け入れ条件**：完了の定義は何か。ユーザー向けの変更であれば、ユーザー自身がブラウザや実機で確認できるか、それともテストやコードレビューのみで確認するかを確認する。
- **制約・前提**：技術的・ビジネス的な制約はあるか。
- **隠れたスコープ**：Issue に明記されていないが、記載された変更に伴って必要になる変更はないか（CI/ワークフロー設定、その他の設定ファイル、隣接するテスト種別（例：ユニットテストに加えて E2E テスト）、ドキュメントなど）。候補を洗い出し、対象外と決めつけずにユーザーに確認する。

Issue にすでに明確な目的・スコープ・受け入れ条件が揃っている場合（例：過去にブラッシュアップ済み）は、質問をスキップして出力形式のまとめに進む。

### 4. ユーザーに質問する

ステップ3で洗い出した不明確な点それぞれについて、解決するまで徹底的にユーザーに質問する。曖昧・部分的な回答で切り上げず、その論点を掘り下げる追加質問をする。

- 質問は**一度に1つだけ**。複数の質問を1つのメッセージにまとめない。
- ステップ3で洗い出した不明確な点を1つずつ順に潰していく。ユーザーが明示的に「先に進めてよい」と言わない限り、未解決のまま出力形式に進まない。
- コードベースの調査で答えられる質問は、ユーザーに聞かず先に調査する。
- 妥当なデフォルト案がある場合は、それを提示してユーザーに確認・修正してもらう形にする（オープンな質問にしない）。
- すでに自明な論点については、そもそも質問しない。例えば、明らかに一方の選択肢が強いメリットを持つ場合、リポジトリ内の既存設計・実装との一貫性から答えが決まる場合、確立した業界標準で答えが決まる場合など。この場合は自分で判断し、その決定と一文程度の根拠を出力形式に記載して先に進む。確認を待つ必要はない。実際に質問するのは、真にトレードオフがある論点に限る。
- UI・見た目・UX の細部（正確な余白、文言のニュアンス、レイアウトの微調整、操作感など）は、文面の説明だけでユーザーに判断させようとしない。実際に見ないと判断しにくいことが多いため。妥当なデフォルト案を提示し、出力形式に「見た目の確認待ち」である旨を記載して、最終判断は実装後の検証（ブラウザや実機での確認）に委ねる。質問を重ねてここで解決しようとしない。

質問の例：

- 「この Issue の完了条件として、〇〇は含まれますか？」
- 「〇〇の部分は既存の△△と干渉しますが、どう扱いますか？」
- 「ユーザー向けの変更ですか、それとも内部実装のみですか？」
- 「ブラウザや実機でご自身で動作確認できますか、それともテストやコードレビューのみでの確認になりますか？」

### 5. Issue を更新する

ブラッシュアップした内容の書き戻しを提案する。
現在の本文との差分として提示し、書き込む前に確認を取る。

```bash
gh issue edit <number> --body "$(cat <<'EOF'
<refined_body>
EOF
)"
```

## 出力形式

Q&A が完了したら、以下の形式でまとめを出力する：

```markdown
## Issue #<number> ブラッシュアップ結果

### 目的
<1〜2文>

### スコープ
- 含むもの: ...
- 含まないもの: ...

### 受け入れ条件
- [ ] ...
- [ ] ...

### 備考
<制約・前提・未解決の疑問点など>
```

## 注意事項

- Issue を更新する前に必ずユーザーの確認を取る。
- ユーザーが「実装に進む」と言うまでは実装しない。
- 質問は簡潔に。一問一答で深掘りする。
